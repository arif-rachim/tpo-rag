# ============================================================================
# PATH CONFIGURATION
# ============================================================================
# Document storage and model locations
PATH_DOCUMENTS="data/tpo"
PATH_MODEL_EMBEDDER="models/multilingual-e5-large"
PATH_MODEL_RERANKER="models/bge-reranker-v2-m3"
PATH_MODEL_NER="models/bert-base-multilingual-cased-ner-hrl"

# Database storage paths
PATH_VECTOR_DB_STORAGE="data/tpo/chroma-store"
PATH_BM25_INDEX_FILE="data/tpo/bm25_index.pkl"
VECTOR_DB_COLLECTION_NAME="safety_documents"

# ============================================================================
# PORTAL CONFIGURATION (SharePoint Integration)
# ============================================================================
PORTAL_URL="https://portal.jac.mil.ae/"
PORTAL_LDAP_USER="your_username"
# WARNING: Password stored in plain text. Consider using environment-specific secrets management.
PORTAL_LDAP_PASSWORD="your_password"
PORTAL_LDAP_DOMAIN="jac"
PORTAL_LIBRARY_NAME="ExtranetSafetyFileLibrary"

# ============================================================================
# MCP SERVER CONFIGURATION
# ============================================================================
MCP_SERVER_PROTOCOL="http"
MCP_SERVER_HOST="155.121.25.23"
MCP_SERVER_PORT="3222"
MCP_SERVER_NAME="MCP_for_JAC_RAG"

# Instruction files (loaded from config/instructions/)
# Leave empty to use defaults from external files
MCP_SERVER_INSTRUCTION_FILE="config/instructions/mcp_server_instruction.txt"
SEARCH_DOCUMENT_TOOL_DESCRIPTION_FILE="config/instructions/search_document_tool_description.txt"
LIST_DOCUMENTS_TOOL_DESCRIPTION_FILE="config/instructions/list_documents_instruction.txt"

# ============================================================================
# AUTHENTICATION (stdlib-only, no JWT/bcrypt!)
# ============================================================================
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD_HASH=8c6976e5b5410415bde908bd4dee15dfb167a9c873fc4bb8a81f6f2ab448a918
SESSION_TIMEOUT_HOURS=8

# ============================================================================
# FILE UPLOAD CONFIGURATION
# ============================================================================
# Maximum file upload size in MB
MAX_UPLOAD_SIZE_MB=50

# Allowed file types (comma-separated, include the dot)
ALLOWED_FILE_TYPES=.pdf,.docx,.pptx,.xlsx,.xls

# ============================================================================
# CORS CONFIGURATION
# ============================================================================
# Comma-separated list of allowed origins (no spaces!)
CORS_ORIGINS=*

# Production example:
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ============================================================================
# SEARCH CONFIGURATION
# ============================================================================
# Number of results to retrieve before reranking
INITIAL_RETRIEVAL_K=20

# Number of final results after reranking
FINAL_RESULTS_K=10

# Minimum similarity score (0-1)
MIN_SIMILARITY_SCORE=0.3

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log directory
LOG_DIR=logs

# Log file rotation size (MB)
LOG_MAX_BYTES=10485760

# Number of backup log files to keep
LOG_BACKUP_COUNT=5

# ============================================================================
# CHUNKING CONFIGURATION
# ============================================================================
# Chunk size for document splitting (characters)
CHUNK_SIZE=1000

# Chunk overlap (characters)
CHUNK_OVERLAP=200